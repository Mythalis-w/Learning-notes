
```C
#include <stdio.h>

int main() {
    int m, n;
    scanf("%d %d", &m, &n);
    
    int a = m, b = n;
    
    // 辗转相除法
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
    }
    
    printf("%d和%d的最大公约数是%d\n", m, n, a);
    return 0;
}
```
