
# 格式错误

最后一个输出通常没有空格，单独判断即可

```C
for(int i=0;i<n;i++){
        printf("%d",arr_reserve[i]);
        if(i!=n-1) printf(" ");  //单独判断的程序
    }
```

# 二维数组 

```C
int arr[][] //第一个是行数，第二个是列数
```

注意，在去除副对角线的时候，对于一个nn的方阵，其右上角是a\_0 n-1，而不是数学中常见的1 n