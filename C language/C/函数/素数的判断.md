```C
int isPrime(int n){
    if(n==2) return 1;   // 这一行和下一行不能换位，不然2会误判  2是素数
    if(n%2==0) return 0; // 其他偶数都不是素数 
    if(n<=1) return 0;   // 1不是素数
    for(int i=3;i<=sqrt(n);i=i+2){ //此时n只能是奇数，所以i=i+2，跳过偶数
        if(n%i==0) return 0;
    }
    return 1;
}
```

通过上面的函数，可以实现素数的判断。