一般来说，所有的迭代器都有一些固定的方法，如：begin，end方法等

![[迭代器方法.png]]

我们主要拿这个图去关注begin end rbegin rend的位置关系，如图前文写的那些函数都是一个迭代器，注意end的位置是在所有空间的后面的一个空间。

几乎所有的STL容器都提供了迭代器的方法
- `name.begin()` 
	生成指向首元素的迭代器
	
- `name.end()`
	生成指向最后一个元素的下一个空间的迭代器
	
	

