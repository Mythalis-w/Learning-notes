# 1.树
树是一个或多个结点的有限集合

结点拥有的子树数目，称为结点的度（只向下统计，不算父节点），和离散里面的度不是一个定义

**结点**：树中的一个独立单元。
**结点的度**：结点拥有的子树数称为结点的度。
**树的度**：树内各结点度的最大值。
**叶子**：度为 0 的结点或终端结点。
**非终端结点**：度不为 0 的结点。
**双亲和孩子**：结点的子树的根称为该结点的孩子，相应地，该结点称为孩子的双亲。
**层次**：结点的层次从根开始定义，根为第一层，根的孩子为第二层，以此类推。

## 性质
1. 树中所有结点数等于所有结点的度数之和加1
2. 对于度为m的树，第i层上最多有$m^{i-1}$个结点

## 树的遍历
树分为先根遍历、后根遍历
先根遍历对应的是二叉树遍历的前序遍历
后根遍历对应的是二叉树遍历的中序遍历
![[树的遍历.png]]
如上图所示，如果把树转化为二叉树，那么和上面的对应关系是一样的
树的遍历，都是从最左边的子树开始的，然后往后推

## 森林的遍历
森林分为前序和中序遍历
![[森林的遍历.png]]
和上面一样，从左图向右图遍历。

| 树    | 森林   | 二叉树  |
| ---- | ---- | ---- |
| 先根遍历 | 前序遍历 | 前序遍历 |
| 后根遍历 | 中序遍历 | 中序遍历 |

# 2. 二叉树
二叉树（Binary Tree）是 n (n$\ge$ 0) 个结点所构成的集合，它或为空树（n=0），或为非空树。对于非空树 T：
1. 有且仅有一个称为根的结点
2. 除根结点以外的其余结点分为两个互不相交的子集 ($T_1$) 和 \($T_2$)，分别称为 T 的左子树和右子树，且 ($T_1$\) 和 ($T_2$) 本身又都是二叉树。
3. 二叉树每个结点至多只有两棵子树。
4. 二叉树的子树有左右之分，其次序不能任意颠倒。

## 1.二叉树基本结构

![[二叉树基本形态.png]]

## 2.二叉树的性质

- 性质一：二叉树的第i层最多有$2^{i-1} (i\ge1)$个结点
- 性质二：深度为k的二叉树最多有$2^k-1 (k\ge1)$个结点
- 性质三：对于任何非空的二叉树T，如果叶子结点的个数为$n_0$，而度为2的结点数为$n_2$，则$n_0 = n_2+1$ 

## 3. 特殊二叉树

### 满二叉树：![[满二叉树.png]]

### 完全二叉树：![[完全二叉树.png]]
==**或者说：完全二叉树的特点：没有左子树，不能有右子树，上一层没有铺满，不能有下一层**==

比如：![[example1.png]]
这个就不是完全二叉树，因为E结点没有满，所以A的左子树没有满，所以就不是完全二叉树

如果J链接在E的左边，成为E的左子树，那这个就是完全二叉树了

---

- 性质四：具有 $n$ 个结点的完全二叉树的深度为 $\log_2n +1$（向下取整）

- 性质五：如果对一棵有 $n$ 个结点的完全二叉树（其深度为 $\log_2n+1$（向下取整））的结点按层序编号（从第 1 层到第 $\log_2n +1$（向下取整层），每层左到右），则对任一结点 $i$（$1 \le i \le n$），以下结论成立。
  - （1）如果 $i=1$，则结点 $i$ 是二叉树的根，无双亲；如果结点 $i>1$则其双亲是结点 $i/2$（向下取整）
  - （2）如果 $2i>n$，则结点 $i$ 无左孩子（结点 $i$ 为叶子结点）；否则其左孩子是结点 $2i$
  - （3）如果 $2i+1>n$，则结点 $i$ 无右孩子；否则其右孩子的结点是 $2i+1$。

继续举例子：![[题目1.png]]
第六层不一定只有8个，第六层最多有32个结点，8个叶结点，24个非叶子结点，这个24个引出的第七层会铺满。

题目二：
![[题目二.png]]
首先，因为是偶数个节点，根据$n=2n_2+n_1+1$，因此可以推算$n_1$为1（且$n_1$只能为1/0），然后推算出$n_2$，再回代推算出$n_0$，选C

## 4.二叉树的储存结构和遍历
如果用顺序结构，除了满二叉树和完全二叉树，其余的二叉树会比较浪费空间

所以我们更加推崇用链式结构![[二叉树的链式结构.png]]

```C
// 定义结构体
typedef char ElemType;

typedef struct TreeNode{
	ElemType data;
	struct TreeNode *lchild;
	struct TreeNode *rchild;
}TreeNode;

typedef TreeNode* BiTree;

'''
先访问根结点，向树的左下方移动，直到遇到空结点为止，然后访问空结点的父结点。接着继续遍历该结点的右子树，如果右子树没有子树可以遍历，那么继续遍历上一层最后一个未被访问的结点。

先序（根左右）：访问根 → 左子树 → 右子树
中序（左根右）：一直往左走到空，回退访问根，再走右子树
后序（左右根）：左→右全部走完，最后才访问根
'''

// 遍历二叉树，自己调用自己，递归调用，这个是一个前序遍历
void preOrder(BiTree T){
	if(T==NULL){
		return;
	}
	printf("%c",T->data);
	preOrder(T->lchild);
	preOrder(T->rchild);
}

// 中序遍历
void inOrder(BiTree T){
	if(T == NULL){
		return;
	}
	inOrder(T->lchild);
	printf("%c",T->data);
	inOrder(T->rchild);
}

// 后序遍历
void inOrder(BiTree T){
	if(T == NULL){
		return;
	}
	inOrder(T->lchild);
	inOrder(T->rchild);
	printf("%c",T->data);
}

```
![[遍历二叉树实例.png]]

前序遍历： A B D H K E C F I G J（从根开始，左遍历，然后右遍历）   //根左右
中序遍历： H K D B E A I F C G J（从最左侧开始）                              //左根右
后序遍历： K H D E B I F J G C A                                                         //左右根

再举两个例子：![[example2.png]]

![[example3.png]]

二叉树的遍历有两个性质：
- 已知前序遍历和中序遍历，可以唯一确定一棵二叉树。
- 已知中序遍历和后序遍历，可以唯一确定一棵二叉树。
- ==注意：已知前序遍历和后序遍历，**不能唯一确定**二叉树。==
  
性质一：前序序列得到根节点，根节点在中序序列中划分左右子树。得到左右子树后，回去前序序列又找到左右子树的根节点，左右子树的根节点又能在中序序列中划分更小的左右子树。

性质二：![[遍历结果推导树性质二.png]]

@ 三种遍历的顺序？或者记忆方法？

****
一些题目：
![[text1.png]]
****

## 5. 线索二叉树

通过遍历可以得到二叉树的线性排列
但这样线性序列只有在遍历时才能得到
将二叉树线索化得到线索二叉树可以解决这个问题
线索化：利用叶节点的空余空间记录前驱、后继

我们先确定一个二叉树和一个遍历顺序，然后利用二叉树中剩余的空位并且依据这个遍历顺序去记录前驱、后继
一个二叉树中如果有n个节点，那么他就一定有n+1个空余空间的节点

例子：![[线索二叉树.png]]

从代码的角度，我们要去区分线索还是左/右子树

```C
typedef char ElemType;

typedef struct ThreadNode{
	ElemType data;
	struct ThreadNode *lchild;
	struct ThreadNode *rchild;
	int ltag;  // sign，表示是左子树还还是线索，1表示指向后继，0表示是指的树
	int rtag;  // sign，表示是右子树还还是线索，1表示指向后继，0表示是指的树
}ThreadNode;

typedef ThreadNode* ThreadTree;
```

在做线索二叉树的时候，需要做四件事情：![[中序遍历线索化.png]]
举一个完整的例子：

```C
typedef char ElemType;

typedef struct ThreadNode{
	ElemType data;
	struct ThreadNode *lchild;
	struct ThreadNode *rchild;
	int ltag;  // sign，表示是左子树还还是线索，1表示指向前驱，0表示是指的树
	int rtag;  // sign，表示是右子树还还是线索，1表示指向后继，0表示是指的树
}ThreadNode;

typedef ThreadNode* ThreadTree;

char str[]="ABDH##I##EJ###CF##G##"
int idx=0;
ThreadTree prev;
 
//创建树
void createTree(ThreadTree *T){
	ElemType ch;
	ch = str[idx++];
	if(ch=='#'){
		*T = NULL;
	}
	else{
		*T =(ThreadTree)malloc(sizeof(ThreadNode));
		(*T)->data = ch;
		createTree(&(*T)->lchild);
		if((*T)->lchild != NULL){
			(*T)->ltag=0;
		}
		createTree(&(*T)->rchild);
		if((*T)->rchild != NULL){
			(*T)->rtag=0;
		}
	}
}

//开始线索化
void inOrderThreading(ThreadTree *head, ThreadTree T)
{
    //头结点
    *head = (ThreadTree)malloc(sizeof(ThreadNode));
    (*head)->ltag = 0;
    (*head)->rtag = 1;
    (*head)->rchild = (*head);

    if (T == NULL){
            (*head)->lchild = *head;
    }
    else{
        (*head)->lchild = T;
        prev = (*head);

        threading(T);

        //最后一个结点线索化
        prev->rchild = *head;
        prev->rtag = 1;

        //头结点右孩子指向最后一个结点
        (*head)->rchild = prev;
    }
}

//具体线索化
void threading(ThreadTree T)
{
    if (T != NULL)
    {
        threading(T->lchild);
        if (T->lchild == NULL)
        {
            T->ltag = 1;
            T->lchild = prev;
        }
        if (prev->rchild == NULL)
        {
            prev->rtag = 1;
            prev->rchild = T;
        }
        prev = T;
        threading(T->rchild);
    }
}

void inOrder(ThreadTree T)
{
    ThreadTree curr;
    curr = T->lchild;

    while(curr != T)
    {
        while(curr->ltag == 0)
        {
            curr = curr->lchild;
        }
        printf("%c ", curr->data);

        while(curr->rtag == 1 && curr->rchild != T)
        {
            curr = curr->rchild;
            printf("%c ", curr->data);
        }
        curr = curr->rchild;
    }
    printf("\n");
}

int main(int argc, char const *argv[])
{
    ThreadTree head;
    ThreadTree T;
    //创建
    createTree(&T);
    //线索化
    inOrderThreading(&head, T);
    //基于线索遍历
    inOrder(head);

    return 0;
}
```

题目：![[example4.png]]
后序遍历是：d b c a
d有两个空，前驱是NULL，后继是b
b只有前驱，是d
c有两个空，前驱是b，后继是a
a没有空
****

![[example5.png]]
中序遍历：d e b x a c
所以是D