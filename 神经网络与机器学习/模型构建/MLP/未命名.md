import torch.nn as nn

class BinaryClassifierMLP(nn.Module):
    def __init__(self, input_dim):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, 128),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(128, 64),
            nn.ReLU(),
            nn.Linear(64, 1),  # 输出1个值
            nn.Sigmoid()       # ← 关键：Sigmoid将输出压缩到(0,1)之间
        )
    
    def forward(self, x):
        return self.net(x)  # 输出就是概率

# 使用示例
model = BinaryClassifierMLP(input_dim=10)
output = model(x_test)  # shape: (batch_size, 1)，值在0-1之间
probability = output.cpu().numpy()  # 这就是概率

这个是二分的MLP吗？